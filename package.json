{
  "name": "banka-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "nyc --reporter=html --reporter=text mocha ./server/test/*.js --require @babel/register  --timeout 5000 --exit",
    "server:start": "nodemon --exec babel-node ./server/index.js",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "clean": "rm -rf build && mkdir build",
    "compile": "./node_modules/.bin/babel ./server -d ./build",
    "build": "npm run clean && npm run compile",
    "start": "node build/index.js"
  },
  "author": "ME",
  "license": "ISC",
  "dependencies": {
    "babel-preset-env": "^1.7.0",
    "babel-register": "^6.26.0",
    "express": "^4.16.4",
    "joi": "^14.3.1",
    "nodemon": "^1.18.11"
  },
  "devDependencies": {
    "@babel/cli": "^7.4.3",
    "@babel/core": "^7.4.3",
    "@babel/node": "^7.2.2",
    "@babel/preset-env": "^7.4.3",
    "@babel/register": "^7.4.0",
    "babel-core": "^6.26.3",
    "chai": "^4.2.0",
    "chai-http": "^4.2.1",
    "coveralls": "^3.0.3",
    "mocha": "^6.1.3",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^13.3.0"
  }
}
